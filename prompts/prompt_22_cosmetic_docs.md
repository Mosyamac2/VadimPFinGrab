# Промпт 22. Документация и косметика после серии Patch 16–21

## Цель
Догнать документацию до фактического состояния кода после серии патчей: убрать акцент на распаковку RAR (e-disclosure отдаёт только ZIP), задокументировать поле `profile`, дописать раздел про tickers.yaml/валидацию, обновить переменные окружения и таблицу источников. Никаких изменений в логике — только пояснения и шаблоны.

## Контекст
- `PLAN_e-disclosure_parser_v2.md` раздел 7, Patch 22.
- Запускается **последним** в серии: к этому моменту все поведенческие изменения (Discoverer, Classifier, Metric Extractor, профили) уже внесены, и документации нужно их догнать.

## Задачи

### 1. README.md
- В разделе системных требований убрать `unrar`/`rar` из обязательных пакетов, перенести в «опциональное» с примечанием: «текущие материалы e-disclosure поставляются ZIP-архивами; rarfile/unrar присутствуют как страховка для устаревших публикаций».
- Добавить раздел «Профили эмитентов» с краткой таблицей: какие тикеры — `bank`, какие — `non_bank`, и какие метрики собираются для каждого профиля.
- Добавить раздел «Сборка `config/tickers.yaml`» с указанием на `tools/find_e_disclosure_ids.py` и `tools/validate_tickers.py` (см. Patch 20).
- В таблицу «Источники метрик» добавить строку про Issuer Report (type=5) — fallback после IFRS/RSBU.

### 2. USER_GUIDE.md
- В разделе «Установка системных пакетов» сделать `unrar` опциональным (с упоминанием `apt install unrar` только для тех, кто хочет страховаться).
- В разделе «Настройка config/tickers.yaml» дописать про `profile`. Привести пример банковской и небанковской записи.
- В разделе «Что попадает в Excel» переписать описание листа `metrics`: набор метрик зависит от `profile`.
- В разделе «Запуск edx update» упомянуть, что перед первым запуском нужно прогнать `python tools/validate_tickers.py`.
- В разделе «Когда что-то не так» добавить два сценария: «`edx update` вернул `0 publications` — проверьте `e_disclosure_id`» и «в Excel пустые `revenue`/`ebitda` — проверьте `profile`, для банков они и не должны заполняться».

### 3. .env.example
- Без изменений в составе переменных. Только привести в соответствие комментарии: упоминание `ANTHROPIC_API_KEY` / `OPENROUTER_API_KEY` / `GOOGLE_OAUTH_*` в одну формулировку с README.

### 4. config/app.yaml.example
- Привести `classifier` секцию в соответствие с Patch 18 (новое поле `min_text_chars_per_page`).
- Привести `text_extractor` секцию в соответствие с Patch 21 (`issuer_trim_max_chars`).
- Поле `discoverer.user_agent` и `discoverer.cookies` уже задокументированы — оставить как есть.

### 5. config/metrics.yaml.template
- Создать (или обновить) `.template`-версию с двумя профилями. В шапке — комментарий: «при добавлении нового тикера-банка убедитесь, что у него `profile: bank` в tickers.yaml; иначе будет считаться корпоратом и метрики будут пустые».

### 6. CHANGELOG (если ведётся)
- Добавить раздел «v0.2 — реальный e-disclosure» с перечислением Patch 16–21 в одну строку каждый.

### 7. prompts/README.md
- Дописать индексную табличку: что делает каждый из патчей 16–22, рекомендованный порядок исполнения (17 → 20 → 16 → 18 → 19 → 21 → 22), какие из них breaking-changes по конфигам (Patch 19 ломает старый плоский `metrics.yaml`).

## Тесты, которые должны проходить
- `make lint typecheck test` — без регрессий.
- Документация не ломает существующие линтеры (markdown lint — если он подключён).
- Шаблоны `app.yaml.example` и `metrics.yaml.template` валидно парсятся Pydantic-моделями (если в pytest есть smoke-тест на это — добавить, иначе ручная проверка в DoD).

## Definition of Done
- README.md и USER_GUIDE.md согласованы с фактическим поведением кода после Patch 16–21.
- `.template`-версии конфигов отражают новую структуру (`profiles:` в metrics, `profile:` в tickers).
- Свежий читатель, проходящий USER_GUIDE с нуля, может развернуть систему и получить корректный Excel **без обращения к диффу прошлых патчей**.
